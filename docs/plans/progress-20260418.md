# Progress 2026-04-18

## Current plan file
- `docs/plans/00-top-down-roadmap.md`

## Current phase / step
- Interpreted roadmap execution as:
  - Phase 0 complete (planning docs already present)
  - Started Phase 1 and the Phase 2 foundation that unblocks later plans

## Modified files
- `Cargo.toml`
- `src/lib.rs`
- `src/error.rs`
- `src/config.rs`
- `src/crc.rs`
- `src/storage/*`
- `src/layout/*`
- `src/blob/*`
- `src/kv/*`
- `src/tsdb/*`
- `tests/storage_region.rs`
- `tests/align.rs`
- `tests/status_codec.rs`
- `tests/crc_compat.rs`
- `tests/layout_kv.rs`
- `tests/layout_ts.rs`

## Completion criteria for this slice
- Crate skeleton exists and compiles.
- Core modules and placeholder boundaries follow the roadmap/plan 01 structure.
- Storage region, alignment, CRC, and initial header/status codecs have unit coverage.

## Notes / deviations
- `layout/status.rs` currently implements a simple monotonic status-table codec that preserves append-only programming semantics, but it is still an initial Rust foundation rather than a byte-for-byte clone of all original FlashDB status encodings.
- `storage/file_sim.rs`, `blob/*`, `kv/*`, and `tsdb/*` are scaffolded only; behavior-heavy phases remain for later plans.

## Next start conditions
- Refine status-table compatibility against upstream FlashDB helpers.
- Add aligned-write helpers and more detailed storage adapter tests.
- Continue with `docs/plans/02-storage-layout-and-status-foundation.md` and `03-blob-and-codec-layer.md`.

## Plan 01 re-run notes
- Re-audited `docs/plans/01-workspace-and-crate-structure.md` against the current crate surface.
- Confirmed the overall crate/module skeleton from plan 01 is present: `lib.rs`, `error.rs`, `config.rs`, `crc.rs`, `storage/*`, `layout/*`, `blob/*`, `kv/*`, and `tsdb/*`.
- Found that the blob module existed only as a thin placeholder and lacked the basic buffer-side type expected by the module-responsibility split.
- Added a minimal `BlobBuf` type plus constructors/accessors for `BlobLocator` and `BlobRef` so the blob layer now has a clearer structure-first API surface without pretending payload IO semantics are implemented.
- Added `tests/blob_layer.rs` to exercise the Blob locator/ref/buffer surface.
- Added `tests/config_validation.rs` to cover crate-level configuration invariants that were previously untested.
- Tightened `TsdbConfig::validate()` so `BlobMode::Fixed(0)` is rejected instead of silently accepting an invalid fixed-size mode.

## Verification for the re-run
- `cargo test`
- `cargo test --features std`

## Remaining gaps after the re-run
- `src/storage/file_sim.rs` is still only a std-gated scaffold; plan 01 structure is present, but simulator behavior belongs to later plans.
- `kv/*` and `tsdb/*` remain intentionally skeletal placeholders; that is still consistent with plan 01’s structure-first goal.
- Higher-value next tests now belong mostly to plan 02: storage offset mapping, aligned writes, layout common constants, and storage adapter behavior.
