[package]
name = "flashdb-for-rust"
version = "0.1.0"
edition = "2021"
description = "FlashDB-inspired storage primitives for Rust"
license = "MIT"
repository = "https://example.invalid/flashdb-for-rust"

[features]
default = []
std = []

[dependencies]
embedded-storage = "0.3"
